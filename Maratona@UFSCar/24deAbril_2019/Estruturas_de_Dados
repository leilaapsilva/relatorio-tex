



ED: uma coleção tanto de valores (e seus relacionamentos) quanto de operações (sobre os valores e estruturas decorrentes).


C++:
  Vector -> Vetor Dinâmico
    #include <vector>
    push_back()  // coloca no final
    size()      //  recupera tamanho
    empty()    //   Diz se size() == 0
    erase()   //    Apaga uma posição específica  
    insert() //     




Pilha: LIFO
  void empilha(Pilha p, Elem x)
    
  Elem  desempilha(Pilha p)                                                               %URI 1062
  Elem  topo(Pilha p)
  bool isVazia()
  bool isCheia()
  
  
  
Fila: FIFO
  void Insere(Fila Q, Elem x)
  int  Remove(Fila Q)
  isCheia(Fila Q)
  isVazia(Fila Q)
  
  Map: 
    map <string, int> m;                                                                   %  URI 1281
    m["banana"] = 2;
    
    
  
